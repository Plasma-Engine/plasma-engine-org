name: CI - Security Scans

on:
  schedule:
    - cron: '0 6 * * 1' # Weekly Monday at 06:00 UTC
  workflow_dispatch:

jobs:
  security:
    uses: ./.github/workflows/../ci/reusable/security.yml
    with:
      python: true
      node: true


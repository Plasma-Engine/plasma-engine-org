//
// Explainer: Provider configuration placeholders.
// Uncomment and configure only the providers you need. Keep credentials out of
// state by using environment variables or a secrets manager.
//

// provider "aws" {
//   region  = var.aws_region
//   profile = var.aws_profile // TODO(infra@plasma): Remove if using role-based auth in CI
// }

// provider "google" {
//   project = var.gcp_project
//   region  = var.gcp_region
// }

// provider "azurerm" {
//   features {}
//   subscription_id = var.azure_subscription_id
// }

